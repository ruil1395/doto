# Dota 2 Counter Bot v2.0

Telegram бот с ML-предсказаниями матчей, контрпиками и статистикой из OpenDota API.

## 🎮 Возможности

- **Контрпики и билды** — база знаний по всем героям
- **Актуальная статистика** — винрейты, тиры, мета (OpenDota API)
- **ML-предсказания** — анализ составов и предсказание победителя
- **Текущая мета** — топ пиков, винрейтов, тренды

## 🚀 Установка

```bash
# Клонирование
git clone https://github.com/USERNAME/dota2-counter-bot.git
cd dota2-counter-bot

# Установка зависимостей
pip install -r requirements.txt

# Настройка
cp .env.example .env
# Отредактируй .env, добавь BOT_TOKEN от @BotFather

# Запуск
python -m src.main
